import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mine',
  appName: 'angular-pwa',
  webDir: 'dist/angular-starter/browser',
  bundledWebRuntime: false
};

export default config;
